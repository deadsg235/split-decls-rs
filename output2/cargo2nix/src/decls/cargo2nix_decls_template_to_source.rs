pub use decls :: * ; # [cfg (feature = "introspector_decl2_macros")] pub use introspector_decl2_macros :: * ; prelude ! { } # [decl_ cargo2nix_decls_template_to_source] fn to_source (pkg : & ResolvedPackage < '_ > , cwd : & Path) -> Result < Source > { let id = pkg . pkg . package_id () ; let source = if id . source_id () . is_registry () { Source :: CratesIo { sha256 : pkg . checksum . as_ref () . map (| c | c . to_string ()) . ok_or (anyhow ! ("checksum is required for crates.io package {}" , id)) ? , } } else if id . source_id () . is_git () { let branch = if let Some (GitReference :: Branch (branch)) = id . source_id () . git_reference () { Some (branch . to_owned ()) } else { None } ; Source :: Git { url : id . source_id () . url () . to_string () , rev : id . source_id () . precise_git_fragment () . map (| p | p . to_string ()) . ok_or (anyhow ! ("precise ref not found for git package {}" , id)) ? , branch , } } else if id . source_id () . is_path () { Source :: Local { path : pathdiff :: diff_paths (Path :: new (id . source_id () . url () . path ()) , cwd) . map (| p | { if p . to_string_lossy () . len () == 0 { p . join (".") } else { p } }) . ok_or (anyhow ! ("path is not absolute for local package {}" , id)) ? , } } else if id . source_id () . is_registry () { Source :: Registry { index : id . source_id () . url () . to_string () , sha256 : pkg . checksum . as_ref () . map (| c | c . to_string ()) . ok_or (anyhow ! ("checksum is required for alternate registry package {}" , id)) ? , } } else { return Err (anyhow ! ("unsupported source for {}" , id)) ; } ; Ok (source) }