pub use decls :: * ; # [cfg (feature = "introspector_decl2_macros")] pub use introspector_decl2_macros :: * ; prelude ! { } # [decl_ cargo2nix_decls_discovery_find_cargo_locks] pub fn find_cargo_locks (root_path : & Path) -> Result < Vec < PathBuf > > { let mut locks = Vec :: new () ; for entry in WalkDir :: new (root_path) . into_iter () . filter_map (| e | e . ok ()) { if entry . file_type () . is_file () && entry . file_name () == "Cargo.lock" { locks . push (entry . into_path ()) ; } } Ok (locks) }