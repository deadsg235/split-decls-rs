pub use decls :: * ; # [cfg (feature = "introspector_decl2_macros")] pub use introspector_decl2_macros :: * ; prelude ! { } # [decl_ monster_traits_decls_number_properties_divides_into_x_parts] # [doc = " Returns X if `value = N * X`. Otherwise, returns `None`."] pub fn divides_into_x_parts (value : u32 , n : u32) -> Option < u32 > { if n == 0 { return None ; } if value % n == 0 { Some (value / n) } else { None } }