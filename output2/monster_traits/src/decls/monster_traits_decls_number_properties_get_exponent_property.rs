pub use decls :: * ; # [cfg (feature = "introspector_decl2_macros")] pub use introspector_decl2_macros :: * ; prelude ! { } # [decl_ monster_traits_decls_number_properties_get_exponent_property] # [doc = " If `value = base^exponent`, returns `exponent`. Otherwise, returns `None`."] pub fn get_exponent_property (value : u32 , base : u32) -> Option < u32 > { if base == 0 { return if value == 0 { Some (1) } else { None } ; } if base == 1 { return if value == 1 { Some (0) } else { None } ; } let mut current_value = value ; let mut exponent = 0 ; while current_value > 1 { if current_value % base != 0 { return None ; } current_value /= base ; exponent += 1 ; } if current_value == 1 { Some (exponent) } else { None } }