use serde :: Deserialize ; use toml ; use trait_fixer_rules_trait :: { ConfigTrait , Rule } ; prelude ! { } # [decl_ trait_fixer_rules_real_decls_impl_for_ConfigTrait] impl ConfigTrait for Config { fn load () -> Self { let path = std :: env :: var ("TRAIT_FIXER_CONFIG") . unwrap_or_else (| _ | "rules.toml" . to_string ()) ; let content = std :: fs :: read_to_string (path) . expect ("Failed to read rules.toml") ; toml :: from_str (& content) . expect ("Invalid TOML in rules.toml") } fn get_rules (& self) -> & Vec < Rule > { & self . rule } }