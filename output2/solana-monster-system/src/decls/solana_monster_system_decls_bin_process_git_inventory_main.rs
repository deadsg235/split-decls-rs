pub use decls :: * ; # [cfg (feature = "introspector_decl2_macros")] pub use introspector_decl2_macros :: * ; prelude ! { } # [decl_ solana_monster_system_decls_bin_process_git_inventory_main] fn main () -> Result < () , Box < dyn std :: error :: Error > > { let content = fs :: read_to_string ("git_files_inventory.txt") ? ; let mut repos = HashSet :: new () ; for line in content . lines () { if line . ends_with ("/.git") { let repo_path = & line [.. line . len () - 5] ; if let Some (name) = repo_path . split ('/') . last () { repos . insert (name . to_string ()) ; } } } println ! ("Found {} unique git repositories:" , repos . len ()) ; let mut sorted : Vec < _ > = repos . into_iter () . collect () ; sorted . sort () ; for repo in & sorted { println ! ("  {}" , repo) ; } fs :: write ("unique_repos.txt" , sorted . join ("\n")) ? ; println ! ("\nSaved to unique_repos.txt") ; Ok (()) }