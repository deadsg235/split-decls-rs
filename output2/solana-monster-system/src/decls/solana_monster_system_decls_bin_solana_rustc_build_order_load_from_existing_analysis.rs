pub use decls :: * ; # [cfg (feature = "introspector_decl2_macros")] pub use introspector_decl2_macros :: * ; prelude ! { } # [decl_ solana_monster_system_decls_bin_solana_rustc_build_order_load_from_existing_analysis] fn load_from_existing_analysis () -> Result < Vec < CrateInfo > , Box < dyn std :: error :: Error > > { let mut crates = Vec :: new () ; let output = Command :: new ("./monster_rocksdb_loader") . output () ; if let Ok (result) = output { let analysis = String :: from_utf8_lossy (& result . stdout) ; if analysis . contains ("code hashes") { crates . push (CrateInfo { order : 1 , name : "monster-analysis" . to_string () , version : "rocksdb" . to_string () , branch : "main" . to_string () , repo : "monster-protocol" . to_string () , path : "./monster_rocksdb" . to_string () , git_hash : get_git_hash (".") ? , criticality : 9 , }) ; } } let output = Command :: new ("git") . args (& ["submodule" , "status"]) . output () ? ; let submodule_data = String :: from_utf8_lossy (& output . stdout) ; let mut order = 2 ; for line in submodule_data . lines () { let parts : Vec < & str > = line . split_whitespace () . collect () ; if parts . len () >= 2 { let hash = parts [0] . trim_start_matches (& ['+' , '-' , ' '] [..]) ; let path = parts [1] ; let name = path . split ('/') . last () . unwrap_or ("unknown") ; if ! is_critical_for_solana_rustc (name) { continue ; } let branch = get_branch_for_path (path) ? ; let repo = get_repo_for_path (path) ? ; let version = get_version_for_path (path) ? ; let criticality = calculate_criticality (name) ; crates . push (CrateInfo { order , name : name . to_string () , version , branch , repo , path : path . to_string () , git_hash : hash [.. 8] . to_string () , criticality , }) ; order += 1 ; } } Ok (crates) }