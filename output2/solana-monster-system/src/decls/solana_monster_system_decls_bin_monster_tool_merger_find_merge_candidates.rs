pub use decls :: * ; # [cfg (feature = "introspector_decl2_macros")] pub use introspector_decl2_macros :: * ; prelude ! { } # [decl_ solana_monster_system_decls_bin_monster_tool_merger_find_merge_candidates] fn find_merge_candidates (tools : & mut [ToolProfile]) { for i in 0 .. tools . len () { for j in (i + 1) .. tools . len () { let common_primes = tools [i] . prime_signature . iter () . filter (| p | tools [j] . prime_signature . contains (p)) . count () ; let total_primes = (tools [i] . prime_signature . len () + tools [j] . prime_signature . len ()) as f64 ; let similarity = if total_primes > 0.0 { (2.0 * common_primes as f64) / total_primes } else { 0.0 } ; if similarity > 0.8 { tools [i] . merge_candidates . push (tools [j] . name . clone ()) ; tools [j] . merge_candidates . push (tools [i] . name . clone ()) ; } } } }