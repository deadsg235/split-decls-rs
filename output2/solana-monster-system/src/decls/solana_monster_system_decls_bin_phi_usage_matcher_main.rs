pub use decls :: * ; # [cfg (feature = "introspector_decl2_macros")] pub use introspector_decl2_macros :: * ; prelude ! { } # [decl_ solana_monster_system_decls_bin_phi_usage_matcher_main] fn main () { println ! ("=== Phi Usage Pattern Matcher ===") ; let mut usages = extract_crate_usage () ; println ! ("📦 Found {} crates with usage patterns" , usages . len ()) ; calculate_phi_signatures (& mut usages) ; println ! ("\n=== Crate Usage Analysis ===") ; for usage in & usages { if usage . usage_frequency > 2 { println ! ("📦 {} (φ: {}, freq: {}, patterns: {})" , usage . crate_name , usage . phi_signature , usage . usage_frequency , usage . usage_pattern . len ()) ; println ! ("   Used by: {:?}" , usage . used_by) ; println ! ("   Patterns: {:?}" , usage . usage_pattern) ; } } let matches = find_usage_matches (& usages) ; println ! ("\n=== Automatic Implementation Matches ===") ; println ! ("🎯 Found {} potential matches" , matches . len ()) ; for (i , m) in matches . iter () . take (20) . enumerate () { println ! ("{}. {} ↔ {} (confidence: {:.2}, φ-sim: {:.2}, overlap: {:.2})" , i + 1 , m . crate1 , m . crate2 , m . match_confidence , m . phi_similarity , m . usage_overlap) ; } let mut phi_groups : HashMap < u64 , Vec < & CrateUsage > > = HashMap :: new () ; for usage in & usages { phi_groups . entry (usage . phi_signature) . or_default () . push (usage) ; } println ! ("\n=== Phi Signature Groups ===") ; for (phi_sig , group) in phi_groups { if group . len () > 1 { println ! ("🧮 φ = {}: {} crates with identical usage patterns" , phi_sig , group . len ()) ; for usage in group { println ! ("   {}" , usage . crate_name) ; } } } println ! ("\n✨ Phi-based usage analysis complete!") ; }