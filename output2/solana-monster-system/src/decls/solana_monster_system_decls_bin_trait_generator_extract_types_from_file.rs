pub use decls :: * ; # [cfg (feature = "introspector_decl2_macros")] pub use introspector_decl2_macros :: * ; prelude ! { } # [decl_ solana_monster_system_decls_bin_trait_generator_extract_types_from_file] fn extract_types_from_file (file : & File) -> Vec < Declaration > { let mut types = Vec :: new () ; for item in & file . items { match item { Item :: Struct (item_struct) => { let name = item_struct . ident . to_string () ; let (fields , methods) = extract_struct_details (item_struct , file) ; types . push (Declaration :: new_struct (item_struct . clone () , fields , methods , phi_hash (& name) ,)) ; } Item :: Enum (item_enum) => { let name = item_enum . ident . to_string () ; let (variants , methods) = extract_enum_details (item_enum , file) ; types . push (Declaration :: new_enum (item_enum . clone () , variants , methods , phi_hash (& name) ,)) ; } _ => { } } } types }