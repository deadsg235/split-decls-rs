pub use decls :: * ; # [cfg (feature = "introspector_decl2_macros")] pub use introspector_decl2_macros :: * ; prelude ! { } # [decl_ solana_monster_system_decls_bin_comprehensive_symbol_analyzer_scan_directory] fn scan_directory (dir : & Path , global_symbols : & mut HashMap < String , SymbolUsage >) { if let Ok (entries) = fs :: read_dir (dir) { for entry in entries . flatten () { let path = entry . path () ; if path . is_dir () { if let Some (name) = path . file_name () . and_then (| n | n . to_str ()) { if ! name . starts_with ('.') && name != "target" { scan_directory (& path , global_symbols) ; } } } else if path . extension () . map_or (false , | ext | ext == "rs") { process_rust_file (& path , global_symbols) ; } } } }