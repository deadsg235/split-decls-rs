pub use decls :: * ; # [cfg (feature = "introspector_decl2_macros")] pub use introspector_decl2_macros :: * ; prelude ! { } # [decl_ solana_monster_system_decls_bin_real_build_order_main] fn main () -> Result < () , Box < dyn std :: error :: Error > > { println ! ("=== SOLANA RUSTC BUILD ORDER (Real Data Only) ===") ; let makefile_content = fs :: read_to_string ("tools/Makefile") ? ; let rust_src_path = extract_rust_src_path (& makefile_content) ? ; let solana_hash = get_git_hash (& rust_src_path) ? ; let solana_branch = get_git_branch (& rust_src_path) ? ; let submodule_output = Command :: new ("git") . args (& ["submodule" , "status"]) . output () ? ; let submodule_data = String :: from_utf8_lossy (& submodule_output . stdout) ; println ! ("1. [9] solana-rustc | real | {} | agave | {}/compiler | git:{}" , solana_branch , rust_src_path , solana_hash) ; let mut order = 2 ; for line in submodule_data . lines () . take (5) { let parts : Vec < & str > = line . split_whitespace () . collect () ; if parts . len () >= 2 { let hash = parts [0] . trim_start_matches (& ['+' , '-' , ' '] [..]) ; let path = parts [1] ; let name = path . split ('/') . last () . unwrap_or ("unknown") ; if matches ! (name , "rustc-build-sysroot" | "rustc-demangle" | "cargo" | "serde" | "allocator-api2") { let branch = get_git_branch (path) . unwrap_or_else (| _ | "unknown" . to_string ()) ; println ! ("{}. [8] {} | real | {} | real | {} | git:{}" , order , name , branch , path , & hash [.. 8]) ; order += 1 ; } } } Ok (()) }