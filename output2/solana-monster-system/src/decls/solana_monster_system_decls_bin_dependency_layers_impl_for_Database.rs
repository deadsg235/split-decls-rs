pub use decls :: * ; # [cfg (feature = "introspector_decl2_macros")] pub use introspector_decl2_macros :: * ; prelude ! { } # [decl_ solana_monster_system_decls_bin_dependency_layers_impl_for_Database] impl Database { fn analyze_dependency_layers (& self) -> Vec < DependencyLayer > { let mut layers = Vec :: new () ; let solana_root = "/home/mdupont/nix/vendor/rust/platform-tools-agave-rust-solana/vendor/rust-src" ; let cargo_lock_path = format ! ("{}/Cargo.lock" , solana_root) ; let dependencies = self . parse_cargo_lock (& cargo_lock_path) ; let layer_map = self . calculate_dependency_layers (& dependencies) ; for layer_num in 1 ..= 31 { let crates_in_layer : Vec < String > = layer_map . iter () . filter (| (_ , & layer) | layer == layer_num) . map (| (name , _) | name . clone ()) . collect () ; if ! crates_in_layer . is_empty () { layers . push (DependencyLayer { layer : layer_num , crates : crates_in_layer , }) ; } } layers } fn parse_cargo_lock (& self , path : & str) -> HashMap < String , Vec < String > > { let mut dependencies = HashMap :: new () ; if let Ok (output) = Command :: new ("grep") . args (& ["-A" , "5" , "\\[\\[package\\]\\]" , path]) . output () { let content = String :: from_utf8_lossy (& output . stdout) ; let mut current_package = String :: new () ; for line in content . lines () { if line . starts_with ("name = ") { current_package = line . replace ("name = " , "") . trim_matches ('"') . to_string () ; } else if line . starts_with ("dependencies = [") && ! current_package . is_empty () { let deps : Vec < String > = line . replace ("dependencies = [" , "") . replace ("]" , "") . split (',') . map (| s | s . trim () . trim_matches ('"') . to_string ()) . filter (| s | ! s . is_empty ()) . collect () ; dependencies . insert (current_package . clone () , deps) ; } } } dependencies } fn calculate_dependency_layers (& self , dependencies : & HashMap < String , Vec < String > > ,) -> HashMap < String , usize > { let mut layer_map = HashMap :: new () ; let mut visited = HashSet :: new () ; for (name , deps) in dependencies { if deps . is_empty () { layer_map . insert (name . clone () , 1) ; visited . insert (name . clone ()) ; } } for layer in 2 ..= 31 { let mut new_crates = Vec :: new () ; for (name , deps) in dependencies { if ! visited . contains (name) { let max_dep_layer = deps . iter () . filter_map (| dep | layer_map . get (dep)) . max () . unwrap_or (& 0) ; if * max_dep_layer == layer - 1 { new_crates . push (name . clone ()) ; } } } for name in new_crates { layer_map . insert (name . clone () , layer) ; visited . insert (name) ; } } layer_map } }