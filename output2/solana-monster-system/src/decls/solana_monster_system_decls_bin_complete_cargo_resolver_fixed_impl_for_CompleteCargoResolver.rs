pub use decls :: * ; # [cfg (feature = "introspector_decl2_macros")] pub use introspector_decl2_macros :: * ; prelude ! { } # [decl_ solana_monster_system_decls_bin_complete_cargo_resolver_fixed_impl_for_CompleteCargoResolver] impl CompleteCargoResolver { fn new () -> Self { Self { cargo_exports : HashMap :: new () , phi_mappings : HashMap :: new () , resolved : HashMap :: new () , } } fn scan_submodules (& mut self) -> Result < () , Box < dyn std :: error :: Error > > { println ! ("=== Scanning ./submodules for Cargo.toml files ===") ; let submodule_paths = vec ! ["./submodules/BLAKE3" , "./submodules/juniper" , "./lattice-introspector" , "./minizinc-introspector" ,] ; for submodule_path in & submodule_paths { if Path :: new (submodule_path) . exists () { self . scan_cargo_in_path (submodule_path) ? ; } } Ok (()) } fn scan_cargo_in_path (& mut self , path : & str) -> Result < () , Box < dyn std :: error :: Error > > { let cargo_toml_path = format ! ("{}/Cargo.toml" , path) ; if Path :: new (& cargo_toml_path) . exists () { if let Ok (content) = fs :: read_to_string (& cargo_toml_path) { let crate_name = self . extract_crate_name (& content) ; println ! ("Found crate: {} at {}" , crate_name , path) ; let lib_paths = vec ! [format ! ("{}/src/lib.rs" , path) , format ! ("{}/src/main.rs" , path) ,] ; let mut exports = Vec :: new () ; for lib_path in & lib_paths { if Path :: new (lib_path) . exists () { if let Ok (lib_content) = fs :: read_to_string (lib_path) { exports . extend (self . extract_exports (& lib_content)) ; } } } if ! exports . is_empty () { self . add_crate_exports (& crate_name , exports) ; } } } Ok (()) } fn extract_crate_name (& self , cargo_content : & str) -> String { for line in cargo_content . lines () { if line . trim () . starts_with ("name = ") { if let Some (name_start) = line . find ('"') { if let Some (name_end) = line [name_start + 1 ..] . find ('"') { return line [name_start + 1 .. name_start + 1 + name_end] . to_string () ; } } } } "unknown" . to_string () } fn extract_exports (& self , lib_content : & str) -> Vec < String > { let mut exports = Vec :: new () ; for line in lib_content . lines () { let trimmed = line . trim () ; if trimmed . starts_with ("pub fn ") { if let Some (paren_pos) = trimmed . find ('(') { let fn_name = & trimmed [7 .. paren_pos] ; exports . push (fn_name . to_string ()) ; } } if trimmed . starts_with ("pub struct ") { let parts : Vec < & str > = trimmed . split_whitespace () . collect () ; if parts . len () > 2 { exports . push (parts [2] . to_string ()) ; } } } exports } fn add_crate_exports (& mut self , crate_name : & str , exports : Vec < String >) { println ! ("  {} exports: {:?}" , crate_name , exports) ; for export in & exports { let full_name = format ! ("{}::{}" , crate_name , export) ; let phi = calculate_phi_key (export) ; self . phi_mappings . insert (full_name , phi) ; } self . cargo_exports . insert (crate_name . to_string () , exports) ; } fn add_rust_std_library (& mut self) { println ! ("=== Adding Rust std library ===") ; let std_exports : Vec < String > = vec ! ["println" , "format" , "write" , "read_to_string" , "HashMap" , "Vec" , "String" , "Option" , "Result" , "fs" , "Command" ,] . into_iter () . map (| s | s . to_string ()) . collect () ; self . add_crate_exports ("std" , std_exports) ; } fn resolve_all (& mut self) { println ! ("\n=== Resolving all unresolved usages ===") ; let unresolved = vec ! ["fs" , "Command" , "HashMap" , "println" , "format" , "Vec" , "String" , "Option" , "Result" , "serialize" , "deserialize" , "blake3" , "hash" , "juniper" , "GraphQL" ,] ; for usage in & unresolved { let mut best_match = None ; let mut best_score = 0 ; for (full_name , phi) in & self . phi_mappings { let score = similarity_score (usage , full_name) ; if score > best_score { best_score = score ; best_match = Some ((full_name . clone () , * phi)) ; } } if let Some ((full_name , phi)) = best_match { if best_score > 40 { self . resolved . insert (usage . to_string () , (full_name . clone () , phi)) ; println ! ("  {} → {} (φ = {}, score: {})" , usage , full_name , phi , best_score) ; } else { println ! ("  {} → UNRESOLVED (best score: {})" , usage , best_score) ; } } else { println ! ("  {} → NO MATCH" , usage) ; } } } }