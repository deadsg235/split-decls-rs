pub use decls :: * ; # [cfg (feature = "introspector_decl2_macros")] pub use introspector_decl2_macros :: * ; prelude ! { } # [decl_ solana_monster_system_decls_bin_variable_complexity_analyzer_extract_variables_from_line] fn extract_variables_from_line (line : & str) -> Vec < (String , String) > { let mut vars = Vec :: new () ; let trimmed = line . trim () ; if trimmed . starts_with ("let ") { let parts : Vec < & str > = trimmed . split_whitespace () . collect () ; if parts . len () >= 2 { let var_name = parts [1] . trim_end_matches (':') . to_string () ; let var_type = if parts . len () >= 3 && parts [2] != "=" { parts [2] . to_string () } else { "inferred" . to_string () } ; vars . push ((var_name , var_type)) ; } } vars }