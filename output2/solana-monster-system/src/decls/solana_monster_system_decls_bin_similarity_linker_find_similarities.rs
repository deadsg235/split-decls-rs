pub use decls :: * ; # [cfg (feature = "introspector_decl2_macros")] pub use introspector_decl2_macros :: * ; prelude ! { } # [decl_ solana_monster_system_decls_bin_similarity_linker_find_similarities] fn find_similarities (declarations : & [Declaration]) -> Vec < SimilarityLink > { let mut links = Vec :: new () ; for i in 0 .. declarations . len () { for j in (i + 1) .. declarations . len () { let decl1 = & declarations [i] ; let decl2 = & declarations [j] ; let similarity = calculate_similarity (& decl1 . signature , & decl2 . signature) ; if similarity > 0.7 { let link_type = if decl1 . decl_type == decl2 . decl_type { "same_type" . to_string () } else { "cross_type" . to_string () } ; links . push (SimilarityLink { decl1_cas : decl1 . cas_id , decl2_cas : decl2 . cas_id , similarity_score : similarity , link_type , }) ; } } } links }