pub use decls :: * ; # [cfg (feature = "introspector_decl2_macros")] pub use introspector_decl2_macros :: * ; prelude ! { } # [decl_ solana_monster_system_decls_bin_lattice_trait_consumer_build_trait_lattice] fn build_trait_lattice (fragments : & [CodeFragment] , similarity_graph : & HashMap < u64 , Vec < u64 > > ,) -> Vec < TraitLayer > { let mut layers = Vec :: new () ; let mut processed = Vec :: new () ; let mut base_candidates : Vec < _ > = fragments . iter () . collect () ; base_candidates . sort_by_key (| f | f . functions . len ()) ; let base_count = (fragments . len () / 5) . max (1) ; for fragment in base_candidates . iter () . take (base_count) { let layer = TraitLayer { name : format ! ("BaseTrait_{}" , fragment . phi_signature % 1000) , level : 0 , functions : fragment . functions . clone () , dependencies : Vec :: new () , phi_signature : fragment . phi_signature , minimal_addition : format ! ("base: {}" , fragment . functions . join (", ")) , } ; layers . push (layer) ; processed . push (fragment . phi_signature) ; } let mut level = 1 ; while processed . len () < fragments . len () && level < 5 { let mut added_this_level = false ; for fragment in fragments { if processed . contains (& fragment . phi_signature) { continue ; } if let Some (similar) = similarity_graph . get (& fragment . phi_signature) { let mut best_base = None ; let mut min_additions = usize :: MAX ; for & similar_phi in similar { if processed . contains (& similar_phi) { if let Some (base_fragment) = fragments . iter () . find (| f | f . phi_signature == similar_phi) { let additions = extract_minimal_additions (& base_fragment . functions , & fragment . functions ,) ; if additions . len () < min_additions && ! additions . is_empty () { min_additions = additions . len () ; best_base = Some ((base_fragment , additions)) ; } } } } if let Some ((base , additions)) = best_base { let layer = TraitLayer { name : format ! ("Layer{}Trait_{}" , level , fragment . phi_signature % 1000) , level , functions : additions . clone () , dependencies : vec ! [format ! ("BaseTrait_{}" , base . phi_signature % 1000)] , phi_signature : fragment . phi_signature , minimal_addition : additions . join (", ") , } ; layers . push (layer) ; processed . push (fragment . phi_signature) ; added_this_level = true ; } } } if ! added_this_level { break ; } level += 1 ; } layers }