pub use decls :: * ; # [cfg (feature = "introspector_decl2_macros")] pub use introspector_decl2_macros :: * ; prelude ! { } # [decl_ solana_monster_system_decls_bin_trait_generator_extract_struct_details] fn extract_struct_details (item_struct : & ItemStruct , file : & File) -> (Vec < String > , Vec < String >) { let mut fields = Vec :: new () ; let mut methods = Vec :: new () ; for field in & item_struct . fields { if let Some (ident) = & field . ident { fields . push (ident . to_string ()) ; } } for item in & file . items { if let Item :: Impl (item_impl) = item { if let Some (self_ty) = & item_impl . self_ty { let self_ty_deref : & Type = & * * self_ty ; if let Type :: Path (type_path) = self_ty_deref { if type_path . path . segments . last () . map_or (false , | segment | segment . ident == item_struct . ident) { for impl_item in & item_impl . items { if let ImplItem :: Fn (impl_item_fn) = impl_item { methods . push (impl_item_fn . sig . ident . to_string ()) ; } } } } } } } (fields , methods) }