pub use decls :: * ; # [cfg (feature = "introspector_decl2_macros")] pub use introspector_decl2_macros :: * ; prelude ! { } # [decl_ solana_monster_system_decls_bin_phi_usage_matcher_extract_use_statement] fn extract_use_statement (line : & str) -> Option < (String , String) > { let trimmed = line . trim () ; if trimmed . starts_with ("use ") { let use_part = & trimmed [4 ..] ; let crate_part = use_part . split ("::") . next () . unwrap_or (use_part) ; let crate_name = crate_part . split ('{') . next () . unwrap_or (crate_part) . trim () ; let usage_type = if use_part . contains ("::") { if use_part . contains ("{") { "multi_import" } else if use_part . contains ("*") { "glob_import" } else { "specific_import" } } else { "crate_import" } ; Some ((crate_name . to_string () , usage_type . to_string ())) } else { None } }