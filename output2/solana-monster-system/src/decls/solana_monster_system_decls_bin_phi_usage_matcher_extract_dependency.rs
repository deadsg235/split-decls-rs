pub use decls :: * ; # [cfg (feature = "introspector_decl2_macros")] pub use introspector_decl2_macros :: * ; prelude ! { } # [decl_ solana_monster_system_decls_bin_phi_usage_matcher_extract_dependency] fn extract_dependency (line : & str) -> Option < String > { let trimmed = line . trim () ; if trimmed . contains ("=") && ! trimmed . starts_with ("#") && ! trimmed . starts_with ("[") { let parts : Vec < & str > = trimmed . split ('=') . collect () ; if parts . len () >= 2 { let dep_name = parts [0] . trim () . trim_matches ('"') ; if ! dep_name . is_empty () && dep_name != "version" && dep_name != "path" { return Some (dep_name . to_string ()) ; } } } None }