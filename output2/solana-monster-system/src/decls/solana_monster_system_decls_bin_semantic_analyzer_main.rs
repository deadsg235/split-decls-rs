pub use decls :: * ; # [cfg (feature = "introspector_decl2_macros")] pub use introspector_decl2_macros :: * ; prelude ! { } # [decl_ solana_monster_system_decls_bin_semantic_analyzer_main] fn main () -> Result < () , Box < dyn std :: error :: Error > > { let content = fs :: read_to_string ("unique_repos.txt") ? ; let mut all_semantics : HashMap < String , RepoSemantics > = HashMap :: new () ; for line in content . lines () { if ! line . trim () . is_empty () { let semantics = analyze_semantics (line . trim ()) ; all_semantics . insert (semantics . name . clone () , semantics) ; } } let json_output = serde_json :: to_string_pretty (& all_semantics) ? ; fs :: write ("repo_semantics.json" , json_output) ? ; println ! ("Semantic analysis complete. {} repositories analyzed." , all_semantics . len ()) ; println ! ("Output saved to repo_semantics.json") ; Ok (()) }