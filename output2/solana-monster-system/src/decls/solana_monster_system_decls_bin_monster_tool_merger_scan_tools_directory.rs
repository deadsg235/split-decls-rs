pub use decls :: * ; # [cfg (feature = "introspector_decl2_macros")] pub use introspector_decl2_macros :: * ; prelude ! { } # [decl_ solana_monster_system_decls_bin_monster_tool_merger_scan_tools_directory] fn scan_tools_directory () -> Vec < ToolProfile > { let mut tools = Vec :: new () ; if let Ok (entries) = fs :: read_dir ("./src/bin") { for entry in entries . flatten () { let path = entry . path () ; if path . extension () . map_or (false , | ext | ext == "rs") { if let Some (name) = path . file_stem () . and_then (| n | n . to_str ()) { if name . contains ("analyzer") || name . contains ("matcher") || name . contains ("linker") { if let Some (tool) = analyze_tool (& path) { tools . push (tool) ; } } } } } } tools }