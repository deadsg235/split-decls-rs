pub use decls :: * ; # [cfg (feature = "introspector_decl2_macros")] pub use introspector_decl2_macros :: * ; prelude ! { } # [decl_ solana_monster_system_decls_bin_cargo_phi_sum_analyze_crate_ast] fn analyze_crate_ast (code : & str) -> u64 { let struct_count = code . matches ("struct ") . count () as u32 ; let enum_count = code . matches ("enum ") . count () as u32 ; let fn_count = code . matches ("fn ") . count () as u32 ; let impl_count = code . matches ("impl ") . count () as u32 ; let trait_count = code . matches ("trait ") . count () as u32 ; let use_count = code . matches ("use ") . count () as u32 ; let binary_exp = (struct_count + impl_count) / 2 ; let ternary_exp = (enum_count + trait_count) / 3 ; let quinary_exp = fn_count / 5 ; let monster_element = (2_u64 . pow (binary_exp . min (20)) + 3_u64 . pow (ternary_exp . min (15)) + 5_u64 . pow (quinary_exp . min (8)) + 11 * use_count as u64) % 196883 ; euler_phi (monster_element) }