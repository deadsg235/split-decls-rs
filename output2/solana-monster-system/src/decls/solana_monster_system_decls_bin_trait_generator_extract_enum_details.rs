pub use decls :: * ; # [cfg (feature = "introspector_decl2_macros")] pub use introspector_decl2_macros :: * ; prelude ! { } # [decl_ solana_monster_system_decls_bin_trait_generator_extract_enum_details] fn extract_enum_details (item_enum : & ItemEnum , file : & File) -> (Vec < String > , Vec < String >) { let mut variants = Vec :: new () ; let mut methods = Vec :: new () ; for variant in & item_enum . variants { variants . push (variant . ident . to_string ()) ; } for item in & file . items { if let Item :: Impl (item_impl) = item { if let Some (self_ty) = & item_impl . self_ty { let self_ty_deref : & Type = & * * self_ty ; if let Type :: Path (type_path) = self_ty_deref { if type_path . path . segments . last () . map_or (false , | segment | segment . ident == item_enum . ident) { for impl_item in & item_impl . items { if let ImplItem :: Fn (impl_item_fn) = impl_item { methods . push (impl_item_fn . sig . ident . to_string ()) ; } } } } } } } (variants , methods) }