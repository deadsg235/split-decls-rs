pub use decls :: * ; # [cfg (feature = "introspector_decl2_macros")] pub use introspector_decl2_macros :: * ; prelude ! { } # [decl_ solana_monster_system_decls_minizinc_data_structures_impl_for_MiniZincOutput] impl MiniZincOutput { pub fn parse_from_string (output : & str) -> Result < Self , String > { let mut variables = HashMap :: new () ; let mut objective_value = None ; let mut status = SolverStatus :: Unknown ; for line in output . lines () { let line = line . trim () ; if line . contains ("==========") { status = SolverStatus :: Optimal ; } else if line . contains ("UNSATISFIABLE") { status = SolverStatus :: Infeasible ; } else if line . contains ("UNBOUNDED") { status = SolverStatus :: Unbounded ; } else if line . contains ("=") && ! line . starts_with ('%') { if let Some ((key , value)) = line . split_once ('=') { let key = key . trim () . to_string () ; let value_str = value . trim () . trim_end_matches (';') ; if let Ok (parsed_value) = Self :: parse_minizinc_value (value_str) { variables . insert (key , parsed_value) ; } } } } Ok (Self { variables , objective_value , status , }) } fn parse_minizinc_value (value_str : & str) -> Result < MiniZincValue , String > { if value_str == "true" { Ok (MiniZincValue :: Bool (true)) } else if value_str == "false" { Ok (MiniZincValue :: Bool (false)) } else if let Ok (i) = value_str . parse :: < i32 > () { Ok (MiniZincValue :: Int (i)) } else if let Ok (f) = value_str . parse :: < f64 > () { Ok (MiniZincValue :: Float (f)) } else if value_str . starts_with ('[') && value_str . ends_with (']') { let inner = & value_str [1 .. value_str . len () - 1] ; let elements : Result < Vec < _ > , _ > = inner . split (',') . map (| s | Self :: parse_minizinc_value (s . trim ())) . collect () ; Ok (MiniZincValue :: Array (elements ?)) } else { Ok (MiniZincValue :: String (value_str . to_string ())) } } pub fn to_optimal_placement (& self) -> Result < OptimalPlacementSolution , String > { let fiber_placements = self . extract_fiber_placements () ? ; let torus_assignments = self . extract_torus_assignments () ? ; let stabilizer_mappings = self . extract_stabilizer_mappings () ? ; let total_coherence = self . variables . get ("total_coherence") . and_then (| v | { if let MiniZincValue :: Float (f) = v { Some (* f) } else { None } }) . unwrap_or (0.0) ; let monster_group_valid = self . variables . get ("monster_group_valid") . and_then (| v | { if let MiniZincValue :: Bool (b) = v { Some (* b) } else { None } }) . unwrap_or (false) ; Ok (OptimalPlacementSolution { fiber_placements , torus_assignments , stabilizer_mappings , total_coherence , monster_group_valid , }) } fn extract_fiber_placements (& self) -> Result < Vec < FiberPlacement > , String > { Ok (Vec :: new ()) } fn extract_torus_assignments (& self) -> Result < Vec < TorusAssignment > , String > { Ok (Vec :: new ()) } fn extract_stabilizer_mappings (& self) -> Result < Vec < StabilizerMapping > , String > { Ok (Vec :: new ()) } }