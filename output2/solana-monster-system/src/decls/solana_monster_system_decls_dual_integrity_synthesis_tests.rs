pub use decls :: * ; # [cfg (feature = "introspector_decl2_macros")] pub use introspector_decl2_macros :: * ; prelude ! { } # [decl_ solana_monster_system_decls_dual_integrity_synthesis_tests] # [cfg (test)] mod tests { use super :: * ; # [test] fn test_complete_dual_integrity () { let mut synthesis = DualIntegritySynthesis :: new () ; let source = "fn main() { println!(\"hello world\"); }" ; let config = "opt-level = 3\ntarget = \"x86_64\"" ; let private_data = b"sensitive_compilation_data" ; let result = synthesis . verify_complete_integrity (source , config , private_data) ; assert ! (result . is_ok ()) ; if let Ok (integrity) = result { assert ! (integrity . mathematical_sound) ; assert ! (integrity . cryptographically_secure) ; assert ! (integrity . cross_validated) ; assert ! (integrity . principles_satisfied) ; assert_eq ! (integrity . system_integrity , SystemIntegrity :: Complete) ; } } # [test] fn test_mathematical_soundness () { let mut synthesis = DualIntegritySynthesis :: new () ; let source = "fn test() -> i32 { 42 }" ; let config = "debug = true" ; let math_evidence = synthesis . verify_mathematical_soundness (source , config) ; assert ! (math_evidence . is_ok ()) ; if let Ok (evidence) = math_evidence { assert ! (evidence . is_sound ()) ; } } # [test] fn test_cryptographic_security () { let mut synthesis = DualIntegritySynthesis :: new () ; let source = "struct Point { x: i32, y: i32 }" ; let config = "edition = \"2021\"" ; let private_data = b"build_secrets" ; let crypto_evidence = synthesis . verify_cryptographic_security (source , config , private_data) ; assert ! (crypto_evidence . is_ok ()) ; if let Ok (evidence) = crypto_evidence { assert ! (evidence . is_secure ()) ; } } }