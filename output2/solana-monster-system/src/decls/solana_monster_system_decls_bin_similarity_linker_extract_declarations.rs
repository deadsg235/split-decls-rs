pub use decls :: * ; # [cfg (feature = "introspector_decl2_macros")] pub use introspector_decl2_macros :: * ; prelude ! { } # [decl_ solana_monster_system_decls_bin_similarity_linker_extract_declarations] fn extract_declarations (dir : & Path) -> Vec < Declaration > { let mut declarations = Vec :: new () ; if let Ok (entries) = fs :: read_dir (dir) { for entry in entries . flatten () { let path = entry . path () ; if path . extension () . map_or (false , | ext | ext == "rs") { if let Ok (content) = fs :: read_to_string (& path) { for (i , line) in content . lines () . enumerate () { let trimmed = line . trim () ; if let Some (decl) = parse_declaration (trimmed , & path , i) { declarations . push (decl) ; } } } } } } declarations }