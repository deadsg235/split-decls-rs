pub use decls :: * ; # [cfg (feature = "introspector_decl2_macros")] pub use introspector_decl2_macros :: * ; prelude ! { } # [decl_ solana_monster_system_decls_bin_solfunmeme_evolve_meme] fn evolve_meme (accounts : & [AccountInfo] , meme_id : u64) -> ProgramResult { let accounts_iter = & mut accounts . iter () ; let state_account = next_account_info (accounts_iter) ? ; let meme_account = next_account_info (accounts_iter) ? ; let mut state = SolfunmemeState :: try_from_slice (& state_account . data . borrow ()) ? ; let mut meme = MemeEntity :: try_from_slice (& meme_account . data . borrow ()) ? ; meme . viral_power = (meme . viral_power * 110) / 100 ; meme . replication_rate = (meme . replication_rate * 105) / 100 ; state . monster_convergence = (state . monster_convergence + meme . viral_power) % 196883 ; meme . serialize (& mut & mut meme_account . data . borrow_mut () [..]) ? ; state . serialize (& mut & mut state_account . data . borrow_mut () [..]) ? ; msg ! ("🧩 Meme evolved: id={}, new_viral_power={}" , meme_id , meme . viral_power) ; Ok (()) }