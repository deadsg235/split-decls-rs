pub use decls :: * ; # [cfg (feature = "introspector_decl2_macros")] pub use introspector_decl2_macros :: * ; prelude ! { } # [decl_ solana_monster_system_decls_bin_monster_rocksdb_similarity_extract_declarations] fn extract_declarations (dir : & Path) -> Vec < MonsterDeclaration > { let mut declarations = Vec :: new () ; if let Ok (entries) = fs :: read_dir (dir) { for entry in entries . flatten () { let path = entry . path () ; if path . is_dir () { declarations . extend (extract_declarations (& path)) ; } else if path . extension () . map_or (false , | ext | ext == "rs") { if let Ok (content) = fs :: read_to_string (& path) { for line in content . lines () { let trimmed = line . trim () ; if let Some (decl) = parse_monster_declaration (trimmed) { declarations . push (decl) ; } } } } } } declarations }