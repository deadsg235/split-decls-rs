pub use decls :: * ; # [cfg (feature = "introspector_decl2_macros")] pub use introspector_decl2_macros :: * ; prelude ! { } # [decl_ solana_monster_system_decls_bin_cargo_phi_sum_calculate_total_phi_sum] fn calculate_total_phi_sum (crate_name : & str , crates : & HashMap < String , CratePhi > , visited : & mut HashMap < String , u64 > ,) -> u64 { if let Some (& cached) = visited . get (crate_name) { return cached ; } if let Some (crate_info) = crates . get (crate_name) { let mut total = crate_info . phi_value ; for dep in & crate_info . dependencies { total += calculate_total_phi_sum (dep , crates , visited) ; } visited . insert (crate_name . to_string () , total) ; total } else { 0 } }