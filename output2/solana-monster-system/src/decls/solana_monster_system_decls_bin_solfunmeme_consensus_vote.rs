pub use decls :: * ; # [cfg (feature = "introspector_decl2_macros")] pub use introspector_decl2_macros :: * ; prelude ! { } # [decl_ solana_monster_system_decls_bin_solfunmeme_consensus_vote] fn consensus_vote (accounts : & [AccountInfo] , meme_id : u64 , vote : bool) -> ProgramResult { let accounts_iter = & mut accounts . iter () ; let meme_account = next_account_info (accounts_iter) ? ; let voter = next_account_info (accounts_iter) ? ; let mut meme = MemeEntity :: try_from_slice (& meme_account . data . borrow ()) ? ; if vote { meme . paxos_score += 1 ; msg ! ("✓ Consensus vote: meme_id={}, score={}" , meme_id , meme . paxos_score) ; } else { if meme . paxos_score > 0 { meme . paxos_score -= 1 ; } msg ! ("✗ Consensus vote: meme_id={}, score={}" , meme_id , meme . paxos_score) ; } meme . serialize (& mut & mut meme_account . data . borrow_mut () [..]) ? ; Ok (()) }