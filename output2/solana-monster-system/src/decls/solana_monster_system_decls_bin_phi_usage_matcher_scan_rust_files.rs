pub use decls :: * ; # [cfg (feature = "introspector_decl2_macros")] pub use introspector_decl2_macros :: * ; prelude ! { } # [decl_ solana_monster_system_decls_bin_phi_usage_matcher_scan_rust_files] fn scan_rust_files (dir : & Path , usage_map : & mut HashMap < String , CrateUsage >) { if let Ok (entries) = fs :: read_dir (dir) { for entry in entries . flatten () { let path = entry . path () ; if path . is_dir () { scan_rust_files (& path , usage_map) ; } else if path . extension () . map_or (false , | ext | ext == "rs") { if let Ok (content) = fs :: read_to_string (& path) { for line in content . lines () { if let Some ((crate_name , usage_type)) = extract_use_statement (line) { let entry = usage_map . entry (crate_name . clone ()) . or_insert_with (| | CrateUsage { crate_name : crate_name . clone () , used_by : Vec :: new () , usage_pattern : Vec :: new () , phi_signature : 0 , usage_frequency : 0 , }) ; entry . usage_pattern . push (usage_type) ; entry . usage_frequency += 1 ; } } } } } } }