use anyhow :: Result ; use serde :: { Deserialize , Serialize } ; prelude ! { } # [decl_ mcp_plugin_traits_decls_McpPlugin] pub trait McpPlugin { # [doc = " Returns the name of the plugin."] fn name (& self) -> & str ; # [doc = " Returns the version of the plugin."] fn version (& self) -> & str ; # [doc = " Executes the plugin's core logic with a given input."] # [doc = " Returns a Result containing the output string or an anyhow::Error."] fn execute (& self , input : & str) -> Result < String > ; # [doc = " Returns the morphological index of the plugin."] fn morphological_index (& self) -> MorphologicalIndex ; }