use sha2 :: { Sha256 , Digest } ; use super :: * ; use crate :: monster_constants ; prelude ! { } # [decl_ monster_multivector_decls_impl_for_MonsterMultivector] impl MonsterMultivector { # [doc = " Creates a new `MonsterMultivector` from a given list of factors."] # [doc = " The factors are sorted and a canonical hash is computed."] # [doc = " Panics if the input `factors` vector does not contain exactly 108 elements."] pub fn new (mut factors : Vec < u32 >) -> Self { if factors . len () != 108 { panic ! ("MonsterMultivector must be constructed with exactly 108 factors.") ; } factors . sort_unstable () ; let canonical_hash = Self :: compute_hash (& factors) ; Self { factors , canonical_hash , } } # [doc = " Returns the canonical `MonsterMultivector` for the Monster Group."] # [doc = ""] # [doc = " This instance is constructed using the hardcoded 108 prime factors"] # [doc = " as specified by the \"108 Supersingular Reasons\" protocol."] pub fn canonical () -> Self { Self :: new (monster_constants :: CANONICAL_MONSTER_FACTORS . to_vec ()) } # [doc = " Computes the SHA256 hash of a sorted list of factors."] fn compute_hash (factors : & [u32]) -> Vec < u8 > { let mut hasher = Sha256 :: new () ; for & factor in factors { hasher . update (factor . to_le_bytes ()) ; } hasher . finalize () . to_vec () } # [doc = " Returns a reference to the sorted list of prime factors."] pub fn factors (& self) -> & [u32] { & self . factors } # [doc = " Returns a reference to the canonical SHA256 hash of the multivector."] pub fn canonical_hash (& self) -> & [u8] { & self . canonical_hash } # [doc = " Checks if this `MonsterMultivector` instance recognizes itself."] # [doc = " This is achieved by re-computing its hash from its current factors"] # [doc = " and comparing it to its stored `canonical_hash`."] # [doc = " In a fully self-consistent system, this should always return `true`."] pub fn recognize_self (& self) -> bool { let current_hash = Self :: compute_hash (& self . factors) ; current_hash == self . canonical_hash } }