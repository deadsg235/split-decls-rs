pub use decls :: * ; # [cfg (feature = "introspector_decl2_macros")] pub use introspector_decl2_macros :: * ; prelude ! { } # [decl_ tool_refactorings_decls_cargo_toml_patcher_config_CargoTomlPatcherConfig] # [doc = " Trait for providing configuration for `Cargo.toml` patching operations."] pub trait CargoTomlPatcherConfig { # [doc = " Returns a map of external dependency names to their corresponding feature flags."] fn get_external_deps (& self) -> & HashMap < String , String > ; # [doc = " Returns a map of local dependency names to their corresponding feature flags."] fn get_local_deps (& self) -> & HashMap < String , String > ; # [doc = " Provides the dependency string for a given external module (e.g., \"{ workspace = true, optional = true }\")."] fn get_external_dep_string (& self , module_name : & str) -> String ; # [doc = " Provides the dependency string for a given local module (e.g., \"{ path = \\\"../crate\\\", optional = true }\")."] fn get_local_dep_string (& self , module_name : & str) -> String ; }