pub use decls :: * ; # [cfg (feature = "introspector_decl2_macros")] pub use introspector_decl2_macros :: * ; prelude ! { } # [decl_ tool_refactorings_decls_code_executor_file_browser_impl_for_CodeExecutorMarkdownFileBrowser] impl < T : CodeExecutorFileBrowserConfigProvider > CodeExecutorMarkdownFileBrowser < T > { pub fn new (config_provider : T) -> Self { gemini_eprintln ! ("Initializing CodeExecutorMarkdownFileBrowser with config:") ; gemini_eprintln ! ("  Viewport size: :size:" , size = config_provider . get_viewport_size ()) ; gemini_eprintln ! ("  Save converted files: :save:" , save = config_provider . get_save_converted_files ()) ; CodeExecutorMarkdownFileBrowser { config_provider , current_path : PathBuf :: from (".") , page_title : String :: new () , page_content : String :: new () , viewport_current_page : 0 , viewport_pages : Vec :: new () , } } pub async fn set_path (& mut self , path : & str) { let new_path = PathBuf :: from (path) ; gemini_eprintln ! ("Setting path to: :path:" , path = new_path . display ()) ; if new_path . exists () { self . current_path = new_path . clone () ; if new_path . is_dir () { self . page_title = format ! ("Directory: {}" , new_path . display ()) ; self . page_content = self . list_directory_content (& new_path) ; } else { let conversion_result = mock_execute_code_blocks (& new_path) ; self . parse_conversion_result (& conversion_result) ; if self . config_provider . get_save_converted_files () { self . save_converted_file (& new_path , & self . page_content) ; } } self . update_viewport_pages () ; } else { self . page_title = "FileNotFoundError" . to_string () ; self . page_content = format ! ("# FileNotFoundError\n\nFile not found: {}" , new_path . display ()) ; self . viewport_pages = vec ! [(0 , self . page_content . len ())] ; } } fn list_directory_content (& self , path : & Path) -> String { let mut content = format ! ("# Contents of {}\n\n" , path . display ()) ; content . push_str ("## Directories\n") ; content . push_str ("- dir_a/\n- dir_b/\n\n") ; content . push_str ("## Files\n") ; content . push_str ("- file1.txt\n- file2.md\n- mock_image.png\n") ; content } fn parse_conversion_result (& mut self , result_string : & str) { let mut title_line = "" ; let mut content_start_idx = 0 ; for (i , line) in result_string . lines () . enumerate () { if line . starts_with ("TITLE:") { title_line = line ; } else if line . starts_with ("CONTENT:") { content_start_idx = i ; break ; } } self . page_title = title_line . strip_prefix ("TITLE:") . unwrap_or ("") . trim () . to_string () ; self . page_content = result_string . lines () . skip (content_start_idx + 1) . collect :: < Vec < & str > > () . join ("\n") ; } fn save_converted_file (& self , original_path : & Path , content : & str) { let work_dir = PathBuf :: from (".") ; let converted_dir = work_dir . join (self . config_provider . get_converted_files_dir_name ()) ; let md_filename = original_path . file_stem () . unwrap_or_default () . to_string_lossy () . to_string () + self . config_provider . get_converted_file_extension () ; let full_path = converted_dir . join (md_filename) ; gemini_eprintln ! ("Simulating saving converted file to: :path:" , path = full_path . display ()) ; } fn update_viewport_pages (& mut self) { self . viewport_pages . clear () ; let viewport_size = self . config_provider . get_viewport_size () ; let mut start = 0 ; let content_len = self . page_content . len () ; while start < content_len { let end = (start + viewport_size) . min (content_len) ; self . viewport_pages . push ((start , end)) ; start = end ; } if self . viewport_pages . is_empty () { self . viewport_pages . push ((0 , 0)) ; } self . viewport_current_page = 0 ; } pub fn get_current_page_content (& self) -> & str { let (start , end) = self . viewport_pages [self . viewport_current_page] ; & self . page_content [start .. end] } pub fn next_page (& mut self) { if self . viewport_current_page + 1 < self . viewport_pages . len () { self . viewport_current_page += 1 ; } } pub fn previous_page (& mut self) { if self . viewport_current_page > 0 { self . viewport_current_page -= 1 ; } } }