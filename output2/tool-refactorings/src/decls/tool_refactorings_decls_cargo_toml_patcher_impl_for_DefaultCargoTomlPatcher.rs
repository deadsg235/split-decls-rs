pub use decls :: * ; # [cfg (feature = "introspector_decl2_macros")] pub use introspector_decl2_macros :: * ; prelude ! { } # [decl_ tool_refactorings_decls_cargo_toml_patcher_impl_for_DefaultCargoTomlPatcher] impl < C : CargoTomlPatcherConfig > DefaultCargoTomlPatcher < C > { pub fn new (config : C) -> Self { Self { config } } fn get_or_create_table_mut < 'a > (doc : & 'a mut toml_edit :: DocumentMut , section : & str) -> & 'a mut Table { if doc . get (section) . is_none () { doc . insert (section , toml_edit :: table ()) ; } doc [section] . as_table_mut () . expect ("Section should be a table after creation/check") } fn add_dependency (doc : & mut toml_edit :: DocumentMut , section : & str , name : & str , dep_str : & str) { let mut deps = Self :: get_or_create_table_mut (doc , section) ; if deps . get (name) . is_none () { let dep_item : Item = dep_str . parse () . unwrap_or_else (| _ | value (dep_str) . into ()) ; deps . insert (name , dep_item) ; } } fn add_feature (doc : & mut toml_edit :: DocumentMut , feature_name : & str , dep_name : & str) { let mut features = Self :: get_or_create_table_mut (doc , "features") ; if features . get (feature_name) . is_none () { features . insert (feature_name , value (toml_edit :: Array :: from_iter (vec ! [format ! ("dep:{}" , dep_name)]))) ; } } }