pub use decls :: * ; # [cfg (feature = "introspector_decl2_macros")] pub use introspector_decl2_macros :: * ; prelude ! { } # [decl_ tool_refactorings_decls_cargo_toml_patcher_CargoTomlPatcher] # [doc = " Trait for patching `Cargo.toml` files."] pub trait CargoTomlPatcher { # [doc = " Patches a `Cargo.toml` file according to predefined rules (standardizing `cargo_metadata`,"] # [doc = " adding external and local dependencies with feature flags)."] # [doc = ""] # [doc = " `cargo_toml_path`: The path to the `Cargo.toml` file to patch."] # [doc = " `dry_run`: If true, no changes are written to the file system."] # [doc = ""] # [doc = " Returns a message indicating the outcome."] fn patch_cargo_toml (& self , cargo_toml_path : & Path , dry_run : bool) -> Result < String , String > ; }